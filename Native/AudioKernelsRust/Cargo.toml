[package]
name = "audio_kernels_rust"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
name = "audio_kernels_rust"
crate-type = ["staticlib", "cdylib"]

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
